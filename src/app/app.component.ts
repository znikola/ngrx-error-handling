import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: `
    <div>app works</div>
    <app-user></app-user>
  `
})
export class AppComponent {
  title = 'ngrx-error-handling';
}
